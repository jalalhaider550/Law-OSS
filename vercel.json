{
  "buildCommand": "npm install --include=dev && cd apps/web && npm install --include=dev && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
