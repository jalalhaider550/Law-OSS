[build]
builder = "nixpacks"
buildCommand = "npm install && npx turbo run build --filter=@law-oss/api..."

[deploy]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
