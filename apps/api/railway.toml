[build]
builder = "nixpacks"
buildCommand = "npm install && cd vendor/db && npx prisma generate && cd ../.. && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
